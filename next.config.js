/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.ibb.co'], // Add 'i.ibb.co' to the array of domains
  },
};

module.exports = nextConfig;
